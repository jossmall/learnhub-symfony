info:
  name: listar cursos nivel avanzado
  type: http
  seq: 4

http:
  method: GET
  url: http://127.0.0.1:8000/api/courses/?nivel=Avanzado
  params:
    - name: nivel
      value: Avanzado
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
